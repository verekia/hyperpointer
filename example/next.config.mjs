/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  reactCompiler: true,
  output: 'export',
  allowedDevOrigins: ['hyperpointer.dev'],
}

export default nextConfig
